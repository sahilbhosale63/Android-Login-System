<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogspot.guestlion.myloginapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/NoActionBarStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CoordinatorLayout"
            android:theme="@style/NoActionBarStyle" />
        <activity android:name=".NavigationDrawerActivity" />
        <activity android:name=".BuildMenu" />
        <activity android:name=".FaceMenu" />
        <activity android:name=".FingerprintMenu" />
        <activity android:name=".ArrowMenu" />
        <activity android:name=".ShareMenu" />
        <activity
            android:name=".DualCamActivity"
            android:theme="@style/NoActionBarStyle"
            android:screenOrientation="landscape"/>
    </application>

</manifest>